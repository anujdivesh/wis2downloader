#!/usr/bin/env bash

ECCODES_VERSION_STR="2.46.0"
ECCODES_MAJOR_VERSION=2
ECCODES_MINOR_VERSION=46
ECCODES_PATCH_VERSION=0
# ECCODES_VERSION="$ECCODES_MAJOR_VERSION.$ECCODES_MINOR_VERSION.$ECCODES_PATCH_VERSION"

ECCODES_GIT_SHA1="c10e8be0c3e27dd2be36a909b06d5d3be6889708"
if [ "x$ECCODES_GIT_SHA1" = "x" ]; then
  ECCODES_GIT_SHA1="unknown"
fi

#################################################################
# Commands
#################################################################
usage()
{
  echo "Usage: $0 [--version] [--info] [--git]"
  exit $1
}

version()
{
  echo "${ECCODES_VERSION_STR}"
}

print_feature()
{
  if [ -z "$1" ]; then
    echo "OFF"
  elif [[ $1 =~ (true|TRUE|ON|1) ]]; then
    echo "ON"
  else
    echo "OFF"
  fi
}

build_type="MinSizeRel"
info()
{
  echo "ecCodes version ${ECCODES_VERSION_STR}, git-sha1 ${ECCODES_GIT_SHA1}"
  echo ""
  echo "Build:"
  echo "  build type      : MinSizeRel"
  echo "  timestamp       : 20260226140610"
  echo "  op. system      : Darwin-25.3.0 (macosx.64)"
  echo "  processor       : arm64"
  echo "  sources         : /opt/actions-runner/work/_work/python-develop-bundle/python-develop-bundle/src/eccodes"
  echo "  c++ compiler    : AppleClang 17.0.0.17000603"
  # Add the most common build type
  if [ $build_type = "RelWithDebInfo" ]; then
    echo "    flags         : -O2 -g -DNDEBUG"
  else
    echo "    flags         :  -pipe"
  fi
#  echo "  fortran compiler: GNU 15.2.0"
#  echo "    flags         : "
  echo ""
  echo "Features:"
  echo "  FORTRAN             : $(print_feature 1)"
  echo "  AEC                 : $(print_feature 1)"
  echo "  MEMFS               : $(print_feature 1)"
  echo "  ECCODES_THREADS     : $(print_feature 1)"
  echo "  ECCODES_OMP_THREADS : $(print_feature 0)"
  echo "  JPG                 : $(print_feature 1)"
  echo "  PNG                 : $(print_feature 1)"
  echo "  NETCDF              : $(print_feature 0)"
  echo "  GEOGRAPHY           : $(print_feature 1)"
  echo "  ECKIT_GEO           : $(print_feature 1)"
  echo ""
  # echo "Dependencies: "
  # if [ -n "" ]; then
  #  echo "  eckit version (2.0.1), git-sha1 $(short_gitsha1 4deb67e8267f7d31edc049b816e84eeb904ff61f)"
  # else
  #  echo "  None"
  # fi
}

gitsha1()
{
  echo "${ECCODES_GIT_SHA1}"
}

short_gitsha1()
{
  if [ -z "$1" ]; then
    echo "unknown"
  else
    echo $1 | head -c 13
  fi
}

#################################################################
# Parse command-line
#################################################################

if test $# -eq 0; then
    info
    # usage 1
fi

while test $# -gt 0; do

    # Split --option=value in $opt="--option" and $val="value"

    opt=""
    val=""

    case "$1" in
    --*=*)
      opt=`echo "$1" | sed 's/=.*//'`
      val=`echo "$1" | sed 's/--[_a-zA-Z0-9]*=//'`
      ;;
    --*)
      opt=$1
      ;;
    *)
      break
      ;;
    esac

    # Parse options
    case "$opt" in
      --version)
        version
        ;;
      --git)
        gitsha1
          ;;
      --info)
        info
        ;;
      --)
        shift
        break
        ;;
      *)
        echo "unknown option: $opt"
        usage 1
        ;;
    esac
    shift
done
